export class Parameter {
    constructor(
        public name: string,
        public value: string,
        public key: string,
        // public source?: string, // commenting for now, may require in future
        ) {}
}